# RC1.3.1 Client Tracking Viewport Safety

Upload this changeset over the current RC1.3 repository. No database migration is required.

This correction:
- reserves bottom safe space for fixed Developer Mode/session controls;
- prevents horizontal layout leakage;
- keeps the tracking timeline fully reachable by normal page scrolling;
- preserves the RC1.3 data and realtime contracts.
