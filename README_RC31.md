# Co Pilot Security Marketplace OS — RC3.1

Run `supabase/migrations/202607260007_rc31_notification_engine.sql`, deploy the changed source files, then complete the acceptance gate.

This package is changes-only and overlays the certified RC3.0 baseline.
