# RC3.2B — Agency Map Module Integration Repair

This package restores the missing `src/modules/maps/AgencyOperationsMap.tsx` module required by both Agency Marketplace and Agency Operations Center.

Upload over the current repository without deleting files. No database migration is required.
