# RC1.4 Real Map Rendering Engine — Acceptance Candidate

This changeset replaces the RC1.3 visual map surface with a real Google Maps renderer.

No Supabase migration is required.

Required Google Cloud APIs:
- Maps JavaScript API
- Directions API
- Places API (already used by property address verification)

Certification remains pending browser acceptance testing in Bolt.
