import { BarChart3, BriefcaseBusiness, Building2, CircleDollarSign, ClipboardList, LayoutDashboard, Mail, Menu, MessageSquareText, Phone, Plus, Send, Settings, ShieldCheck, Users, X } from 'lucide-react';
import { useState, type ReactNode } from 'react';
import { NavLink, useLocation, useNavigate } from 'react-router-dom';
import { usePortfolioStore } from '../store/PortfolioStore';
const ownerNav=[['Command','/',LayoutDashboard],['Portfolios','/portfolios',BriefcaseBusiness],['Create Portfolio','/portfolios/new',Plus],['Outreach','/outreach',Phone],['Distributions','/distributions',Send],['Email Templates','/templates',Mail],['Negotiations','/negotiations',CircleDollarSign],['Agencies','/agencies',Building2],['Employees','/employees',Users],['Analytics','/analytics',BarChart3],['Audit','/audit',ShieldCheck],['Settings','/settings',Settings]] as const;
const employeeNav=[['Today','/employee',LayoutDashboard],['Active Portfolio','/employee/portfolio',BriefcaseBusiness],['Prospect','/employee/prospect',Plus],['Contact Buyer','/employee/outreach',MessageSquareText],['Distribute','/employee/distribute',Send],['My Agencies','/employee/agencies',Building2],['Follow-Ups','/employee/follow-ups',ClipboardList],['Performance','/employee/performance',BarChart3]] as const;
export default function Shell({children}:{children:ReactNode}){const {role,setRole}=usePortfolioStore();const [open,setOpen]=useState(false);const location=useLocation();const navigate=useNavigate();const nav=role==='owner'?ownerNav:employeeNav;function switchRole(next:'owner'|'employee'){setRole(next);setOpen(false);navigate(next==='owner'?'/':'/employee')}return <div className="min-h-screen bg-[#f4f7fb] text-slate-950"><aside className={`fixed inset-y-0 left-0 z-40 flex w-72 flex-col border-r border-slate-800 bg-[#08101f] p-5 text-white transition-transform lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="mb-8 flex items-start justify-between"><div><p className="text-xs font-semibold tracking-[.24em] text-blue-400">DATA MARKET HOUSE</p><h1 className="mt-2 text-xl font-semibold">Sales OS</h1><p className="mt-1 text-xs text-slate-500">Distribution Engine · v0.5.1</p></div><button className="lg:hidden" onClick={()=>setOpen(false)}><X/></button></div><div className="mb-5 grid grid-cols-2 rounded-2xl bg-white/5 p-1"><button onClick={()=>switchRole('owner')} className={`rounded-xl px-3 py-2 text-xs font-semibold ${role==='owner'?'bg-white text-slate-950':'text-slate-400'}`}>Owner</button><button onClick={()=>switchRole('employee')} className={`rounded-xl px-3 py-2 text-xs font-semibold ${role==='employee'?'bg-white text-slate-950':'text-slate-400'}`}>Employee</button></div><nav className="space-y-1 overflow-y-auto">{nav.map(([label,to,Icon])=><NavLink key={label} to={to} onClick={()=>setOpen(false)} className={({isActive})=>`flex items-center gap-3 rounded-2xl px-4 py-3 text-sm transition ${isActive||(to==='/'&&location.pathname==='/')?'bg-white/10 text-white':'text-slate-400 hover:bg-white/5 hover:text-white'}`}><Icon size={18}/>{label}</NavLink>)}</nav><div className="mt-auto rounded-2xl border border-white/10 bg-white/5 p-4"><p className="text-xs text-slate-400">Operating principle</p><p className="mt-1 text-sm font-medium">Engines by engineers.</p></div></aside><main className="lg:pl-72"><div className="sticky top-0 z-30 flex h-16 items-center justify-between border-b border-slate-200/80 bg-[#f4f7fb]/90 px-5 backdrop-blur lg:hidden"><button onClick={()=>setOpen(true)}><Menu/></button><span className="text-sm font-semibold">DMH Sales OS</span><span className="text-xs font-semibold text-blue-600">{role}</span></div>{children}</main></div>}
