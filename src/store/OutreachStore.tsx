import { createContext, useContext, useEffect, useMemo, useState, type ReactNode } from 'react';
export interface EmailTemplate{id:string;name:string;subject:string;body:string;active:boolean;createdAt:string;}
type Store={templates:EmailTemplate[];addTemplate:(input:Omit<EmailTemplate,'id'|'createdAt'>)=>void;toggleTemplate:(id:string)=>void;};
const KEY='dmh-sales-os-outreach-templates-v040';
const seed:EmailTemplate[]=[{id:'intro',name:'Initial portfolio introduction',subject:'Charged-off portfolio opportunity',body:'Hi {{first_name}},\n\nData Market House currently has a charged-off portfolio available for review. I can send a masked sample and summary if this fits your acquisition criteria.\n\nBest,\n{{employee_name}}',active:true,createdAt:new Date().toISOString()},{id:'followup',name:'Sample follow-up',subject:'Following up on the masked portfolio sample',body:'Hi {{first_name}},\n\nI am following up on the masked sample sent for {{portfolio_name}}. Please let me know whether you would like to discuss pricing or documentation.\n\nBest,\n{{employee_name}}',active:true,createdAt:new Date().toISOString()}];
const C=createContext<Store|null>(null);
export function OutreachProvider({children}:{children:ReactNode}){const [templates,setTemplates]=useState<EmailTemplate[]>(()=>{try{return JSON.parse(localStorage.getItem(KEY)||'null')||seed}catch{return seed}});useEffect(()=>localStorage.setItem(KEY,JSON.stringify(templates)),[templates]);const value=useMemo<Store>(()=>({templates,addTemplate:input=>setTemplates(s=>[{...input,id:crypto.randomUUID(),createdAt:new Date().toISOString()},...s]),toggleTemplate:id=>setTemplates(s=>s.map(t=>t.id===id?{...t,active:!t.active}:t))}),[templates]);return <C.Provider value={value}>{children}</C.Provider>}
export function useOutreachStore(){const x=useContext(C);if(!x)throw new Error('OutreachProvider missing');return x}
