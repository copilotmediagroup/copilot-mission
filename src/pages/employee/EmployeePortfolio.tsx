import { Check, Download, LockKeyhole } from 'lucide-react';
import { Card, Pill, PrimaryButton } from '../../components/Primitives';
import { Link } from 'react-router-dom';
import { usePortfolioStore } from '../../store/PortfolioStore';
const money=(n:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n);
export default function EmployeePortfolio(){const {active}=usePortfolioStore();if(!active)return <div className="p-10">No active portfolio.</div>;return <div className="mx-auto max-w-6xl p-5 md:p-8 lg:p-10"><header className="mb-8"><Pill tone="success">Active</Pill><h2 className="mt-3 text-3xl font-semibold">{active.name}</h2><p className="mt-2 text-slate-500">Approved employee sales workspace</p></header><div className="grid gap-6 lg:grid-cols-[1.35fr_.65fr]"><Card className="p-7 md:p-9"><p className="text-sm text-slate-500">Portfolio story</p><p className="mt-3 text-lg leading-8 text-slate-700">{active.description}</p><div className="mt-7 space-y-3">{active.sellingPoints.map(x=><div className="flex gap-3 rounded-2xl bg-slate-50 p-4" key={x}><Check className="text-emerald-600" size={19}/><span className="text-sm font-medium">{x}</span></div>)}</div></Card><div className="space-y-6"><Card className="p-6"><p className="text-sm text-slate-500">Asking price</p><p className="mt-2 text-3xl font-semibold">{money(active.askingPrice)}</p><p className="mt-4 text-sm text-slate-500">Use the approved asking price. All offers still require owner review.</p></Card><Card className="p-6"><p className="font-semibold">Masked sample</p><p className="mt-2 text-sm text-slate-500">{active.file?.name||'No file available'}</p><Link to="/employee/distribute"><PrimaryButton className="mt-5 w-full" disabled={!active.file}><Download className="mr-2" size={17}/>Distribute masked CSV</PrimaryButton></Link><p className="mt-3 text-xs leading-5 text-slate-400">Anonymous downloads are disabled. Select the agency and recipient first.</p></Card><Card className="flex gap-3 p-5"><LockKeyhole className="shrink-0 text-blue-600"/><div><p className="text-sm font-semibold">Owner economics protected</p><p className="mt-1 text-xs leading-5 text-slate-500">Private minimum, acquisition cost and margin do not enter this workspace.</p></div></Card></div></div></div>}
