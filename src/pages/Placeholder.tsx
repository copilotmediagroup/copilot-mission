import { Card } from '../components/Primitives';
export default function Placeholder({title}:{title:string}){return <div className="mx-auto max-w-5xl p-5 md:p-8 lg:p-10"><p className="text-sm font-semibold text-blue-600">Engine roadmap</p><h2 className="mt-1 text-3xl font-semibold">{title}</h2><Card className="mt-8 grid min-h-80 place-items-center p-8 text-center"><div><p className="text-xl font-semibold">Reserved for its complete engine build</p><p className="mx-auto mt-2 max-w-lg text-slate-500">This screen remains intentionally quiet until the full workflow behind it is connected. No fake controls.</p></div></Card></div>}
