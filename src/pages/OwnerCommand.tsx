import { ArrowRight, Flame, Plus } from 'lucide-react';
import { Link } from 'react-router-dom';
import { Card, Pill, PrimaryButton } from '../components/Primitives';
import { usePortfolioStore } from '../store/PortfolioStore';
const money=(n:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n);
export default function OwnerCommand(){const {active,portfolios,audit}=usePortfolioStore();return <div className="mx-auto max-w-[1500px] p-5 md:p-8 lg:p-10"><header className="mb-8 flex flex-col gap-5 md:flex-row md:items-end md:justify-between"><div><p className="text-sm font-semibold text-blue-600">Owner command</p><h2 className="mt-1 text-3xl font-semibold tracking-tight md:text-4xl">The sale, at a glance.</h2><p className="mt-2 text-slate-500">One active campaign. One decision queue. No noise.</p></div><Link to="/portfolios/new"><PrimaryButton><Plus size={18} className="mr-2"/>Create portfolio</PrimaryButton></Link></header>{active?<div className="grid gap-6 xl:grid-cols-[1.55fr_.85fr]"><Card className="overflow-hidden"><div className="p-6 md:p-8"><div className="flex items-start justify-between"><div><div className="flex items-center gap-3"><Pill tone="success">{active.status}</Pill><span className="text-sm text-slate-500">Campaign live</span></div><h3 className="mt-4 text-3xl font-semibold">{active.name}</h3><p className="mt-2 text-slate-500">{active.accountCount.toLocaleString()} accounts · {active.originalCreditor}</p></div><Flame className="text-orange-500"/></div></div><div className="grid gap-px bg-slate-100 sm:grid-cols-3"><Metric label="Asking price" value={money(active.askingPrice)}/><Metric label="Private minimum" value={money(active.privateMinimum)}/><Metric label="Masked file" value={active.file?'Ready':'Missing'}/></div><div className="p-6 md:p-8"><p className="text-sm text-slate-500">Approved sales summary</p><p className="mt-2 max-w-3xl text-lg leading-8 text-slate-700">{active.description}</p><Link to={`/portfolios/${active.id}`} className="mt-6 inline-flex items-center text-sm font-semibold text-blue-600">Open portfolio control <ArrowRight className="ml-2" size={17}/></Link></div></Card><Card className="p-6"><p className="text-sm text-slate-500">Engine health</p><h3 className="mt-1 text-xl font-semibold">Portfolio activation</h3><div className="mt-6 space-y-4"><Health label="Active campaign enforced"/><Health label="Owner-only pricing separated"/><Health label="Masked file recorded" ok={!!active.file}/><Health label="Status transition audit" ok={audit.length>0}/></div></Card></div>:<Card className="grid min-h-[420px] place-items-center p-8 text-center"><div><div className="mx-auto grid h-16 w-16 place-items-center rounded-3xl bg-blue-50 text-blue-600"><Plus/></div><h3 className="mt-5 text-2xl font-semibold">No active portfolio</h3><p className="mx-auto mt-2 max-w-md text-slate-500">Create a portfolio, upload its masked CSV, mark it Ready, then activate the campaign.</p><Link to="/portfolios/new"><PrimaryButton className="mt-6">Create first portfolio</PrimaryButton></Link></div></Card>}<div className="mt-6 grid gap-6 md:grid-cols-3"><MetricCard label="Portfolio queue" value={`${portfolios.length} total`} detail={`${portfolios.filter(p=>p.status==='ready').length} ready to activate`}/><MetricCard label="Private controls" value="Owner only" detail="Minimum and acquisition cost remain protected"/><MetricCard label="Recent engine actions" value={`${audit.length}`} detail="Creation, edits and transitions recorded"/></div></div>}
function Metric({label,value}:{label:string;value:string}){return <div className="bg-white p-6"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-2xl font-semibold">{value}</p></div>}
function MetricCard({label,value,detail}:{label:string;value:string;detail:string}){return <Card className="p-6"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-2xl font-semibold">{value}</p><p className="mt-3 text-sm text-slate-500">{detail}</p></Card>}
function Health({label,ok=true}:{label:string;ok?:boolean}){return <div className="flex items-center justify-between rounded-2xl bg-slate-50 p-4"><span className="text-sm font-medium">{label}</span><Pill tone={ok?'success':'warning'}>{ok?'Ready':'Needs attention'}</Pill></div>}
