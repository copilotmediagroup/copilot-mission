import { Card, Pill } from '../components/Primitives';
import { usePortfolioStore } from '../store/PortfolioStore';
export default function AuditPage(){const {audit}=usePortfolioStore();return <div className="mx-auto max-w-6xl p-5 md:p-8 lg:p-10"><p className="text-sm font-semibold text-blue-600">Audit engine</p><h2 className="mt-1 text-3xl font-semibold">Portfolio activity</h2><Card className="mt-8 overflow-hidden">{audit.length?audit.map(a=><div key={a.id} className="flex flex-col gap-3 border-b border-slate-100 p-6 last:border-0 sm:flex-row sm:items-center"><Pill tone="blue">{a.action}</Pill><p className="flex-1 text-sm font-medium">{a.detail}</p><time className="text-xs text-slate-400">{new Date(a.occurredAt).toLocaleString()}</time></div>):<div className="p-12 text-center text-slate-500">Engine actions will appear here.</div>}</Card></div>}
