import { FormEvent, useState } from 'react';
import { Card, Pill, PrimaryButton, SecondaryButton, inputClass } from '../../components/Primitives';
import { useOutreachStore } from '../../store/OutreachStore';
export default function EmailTemplates(){const {templates,addTemplate,toggleTemplate}=useOutreachStore();const [creating,setCreating]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);addTemplate({name:String(f.get('name')),subject:String(f.get('subject')),body:String(f.get('body')),active:true});setCreating(false)}return <div className="mx-auto max-w-5xl p-5 md:p-8 lg:p-10"><header className="mb-8 flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p className="text-sm font-semibold text-blue-600">Message control</p><h2 className="mt-1 text-3xl font-semibold">Email templates</h2><p className="mt-2 text-slate-500">Employees communicate with approved language, not improvised promises.</p></div><PrimaryButton onClick={()=>setCreating(true)}>Create template</PrimaryButton></header>{creating&&<Card className="mb-6 p-6"><form onSubmit={submit} className="grid gap-4"><input className={inputClass} name="name" required placeholder="Template name"/><input className={inputClass} name="subject" required placeholder="Email subject"/><textarea className={`${inputClass} min-h-56`} name="body" required placeholder="Approved message body"/><div className="flex justify-end gap-3"><SecondaryButton type="button" onClick={()=>setCreating(false)}>Cancel</SecondaryButton><PrimaryButton>Save template</PrimaryButton></div></form></Card>}<div className="space-y-4">{templates.map(t=><Card key={t.id} className="p-6"><div className="flex flex-col gap-4 md:flex-row md:items-start"><div className="flex-1"><div className="flex items-center gap-3"><h3 className="text-lg font-semibold">{t.name}</h3><Pill tone={t.active?'success':'neutral'}>{t.active?'Active':'Inactive'}</Pill></div><p className="mt-3 font-semibold">{t.subject}</p><p className="mt-3 whitespace-pre-line text-sm leading-6 text-slate-500">{t.body}</p></div><SecondaryButton onClick={()=>toggleTemplate(t.id)}>{t.active?'Deactivate':'Activate'}</SecondaryButton></div></Card>)}</div></div>}
