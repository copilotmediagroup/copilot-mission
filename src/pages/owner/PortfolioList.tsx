import { ArrowRight, Plus } from 'lucide-react';
import { Link } from 'react-router-dom';
import { Card, Pill, PrimaryButton } from '../../components/Primitives';
import { usePortfolioStore } from '../../store/PortfolioStore';
const money=(n:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n);
const tone=(s:string):'neutral'|'success'|'warning'|'danger'|'blue'=>s==='active'?'success':s==='ready'?'blue':s==='draft'?'neutral':s==='archived'?'danger':'warning';
export default function PortfolioList(){const {portfolios}=usePortfolioStore();return <div className="mx-auto max-w-[1400px] p-5 md:p-8 lg:p-10"><header className="mb-8 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold text-blue-600">Portfolio engine</p><h2 className="mt-1 text-3xl font-semibold">Portfolio queue</h2><p className="mt-2 text-slate-500">Prepare inventory here. Only one portfolio may control the campaign at a time.</p></div><Link to="/portfolios/new"><PrimaryButton><Plus className="mr-2" size={18}/>Create portfolio</PrimaryButton></Link></header><Card className="overflow-hidden"><div className="hidden grid-cols-[1.4fr_.8fr_.7fr_.7fr_40px] gap-5 border-b border-slate-100 px-6 py-4 text-xs font-semibold uppercase tracking-wider text-slate-400 md:grid"><span>Portfolio</span><span>Status</span><span>Accounts</span><span>Asking</span><span/></div><div className="divide-y divide-slate-100">{portfolios.map(p=><Link key={p.id} to={`/portfolios/${p.id}`} className="grid gap-3 p-6 transition hover:bg-slate-50 md:grid-cols-[1.4fr_.8fr_.7fr_.7fr_40px] md:items-center md:gap-5"><div><p className="font-semibold">{p.name}</p><p className="mt-1 text-sm text-slate-500">{p.originalCreditor} · {p.file?.name||'No masked file'}</p></div><div><Pill tone={tone(p.status)}>{p.status.replace('_',' ')}</Pill></div><p className="text-sm font-medium">{p.accountCount.toLocaleString()}</p><p className="text-sm font-semibold">{money(p.askingPrice)}</p><ArrowRight className="text-slate-300" size={19}/></Link>)}</div></Card></div>}
